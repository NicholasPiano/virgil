
import { Header } from 'semantic-ui-react';
import styled from 'styled-components';

const StyledCharacter = styled(Header)`
  &.ui.header {
    text-align: center;
  }
`;

export default StyledCharacter;
