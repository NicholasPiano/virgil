
import { compose } from 'redux';
import { connect } from 'react-redux';

import JavascriptImplementation from './JavascriptImplementation';

export default compose(
  connect(null, null),
)(JavascriptImplementation);
